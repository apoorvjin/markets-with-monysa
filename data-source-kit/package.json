{
  "name": "data-source-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Self-contained, dependency-free source adapters (upstream endpoint + auth + params + rate-limit + response->normalized mapping). Copy into any project; port to any language.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "try": "tsx scripts/try.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0",
    "tsx": "^4.20.6"
  }
}
